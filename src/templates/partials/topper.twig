{% set topper = site.data['topper.yaml'] %}
{% set benefits = topper.benefits %}

<div class="page-topper">
  <div class="page-topper-content">

    <div class="topper-art js-reveal">
      <canvas id="canvas" class="topper-canvas"></canvas>
    </div>
    
    <div class="topper-title js-reveal">
      <h2 class="js-reveal-stagger-item">{{ topper.title }}</h2>
      <div class="js-topper-deck intro-section js-reveal-stagger-item">
        <p class="intro-paragraph">
          {{ topper.desc }}
        </p>
      </div>
    </div>

    <div class="grid benefits">
      {% for benefit in benefits %}
      <div class="grid-cell list js-topper-list js-reveal">
        <div class="list-content">
          <h4 class="list--title js-reveal-stagger-item">
            {{benefit.title}}
          </h4>
          <ul class="list--items">
          {% for item in benefit.list %}
            <li class="list--item js-reveal-stagger-item">
              {% include '@templates/svgs/checkmark.svg' %}
              {{item}}
            </li>
          {% endfor %}
          </ul>
        </div>
      </div>
      {% endfor %}
      <div class="grid-cell list"></div>
    </div>
  </div>
</div>
